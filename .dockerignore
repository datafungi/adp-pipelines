# The image bakes only the dependency set. DAGs/plugins are mounted at runtime, so keep
# the build context to pyproject.toml + uv.lock (fast, reproducible, no local junk).
*
!pyproject.toml
!uv.lock
